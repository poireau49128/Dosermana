@model Dosermana.WebUI.Models.RegisterModel

@{
    ViewBag.Title = "Register";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@*<h2>Регистрация</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div>
        <h4>Регистрация пользователя</h4>
        <hr />
        @Html.ValidationSummary(false)

        <div>
            Логин
            <div>
                @Html.EditorFor(model => model.Email)
            </div>
        </div>

        <div>
            Адрес доставки
            <div>
                @Html.EditorFor(model => model.Address)
            </div>
        </div>

        <div>
            Пароль
            <div>
                @Html.EditorFor(model => model.Password)
            </div>
        </div>

        <div>
            Подтвердить пароль
            <div>
                @Html.EditorFor(model => model.PasswordConfirm)
            </div>
        </div>

        <div>
            <div>
                <input type="submit" value="Зарегистрировать" />
            </div>
        </div>
    </div>
}*@


<link href="~/Content/Login.css" rel="stylesheet" />


<div class="container">
    <div class="card card-container">
        <img id="profile-img" class="profile-img-card" src="//ssl.gstatic.com/accounts/ui/avatar_2x.png" />
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(false)

            <input type="text" class="form-control" name="Email" placeholder="Логин" required autofocus>
            <input type="text" class="form-control" name="Address" placeholder="Адрес доставки" required>
            <input type="password" class="form-control" name="Password" placeholder="Пароль" required>
            <input type="password" class="form-control" name="PasswordConfirm" placeholder="Подтвердите пароль" required>
            <button class="btn btn-lg btn-primary btn-block btn-signin" type="submit">Зарегистрироваться</button>
        }
        <a href="@Url.Action("Login", "Account")" class="register">
            Войти
        </a>
    </div><!-- /card-container -->
</div><!-- /container -->