@using Dosermana.WebUI.Models
@using Dosermana.WebUI.HtmlHelpers
@model ProductsListViewModel

@{
    ViewBag.Title = "Товары";
}
<link href="~/Content/products.css" rel="stylesheet" />






<div class="body">
    <div id="categories" class="left-nav">
        @Html.Action("Menu", "Nav")
    </div>
    <div class="content">
        <div class="main">
            @foreach (var p in @Model.Products)
            {
                @Html.Partial("ProductSummary", p)
            }
        </div>


        @*<div class="btn-group pull-right">
            @Html.PageLinks(Model.PagingInfo, x => Url.Action("List",
                new { page = x, category = Model.CurrentCategory, subcategory = Model.CurrentSubCategory }))
        </div>*@
        <div class="btn-group pull-right">
            @Html.PageLinks(Model.PagingInfo, x => Url.GetRouteUrlForPage(x))
        </div>
    </div>
</div>







@*<div class="main">
    @foreach (var p in @Model.Products)
    {
        @Html.Partial("ProductSummary", p)
    }
</div>


<div class="btn-group pull-right">
    @Html.PageLinks(Model.PagingInfo, x => Url.Action("List",
        new { page = x, category = Model.CurrentCategory }))
</div>*@