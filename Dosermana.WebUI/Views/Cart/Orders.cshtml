@model IEnumerable<Dosermana.Domain.Entities.Order>

@{
    ViewBag.Title = "Orders";
}

<style>
    #cartTable td {
        vertical-align: middle;
    }
</style>


<h2>Ваши заказы</h2>
<table id="cartTable" class="table">
    <thead>
        <tr>
            <th class="text-center">Продукт</th>
            <th class="text-center">Кол-во</th>
            <th class="text-center">Цена за единицу товара</th>
            <th class="text-center">Общая цена</th>
            <th class="text-center">Статус заказа</th>
            <th class="text-center">Примечание</th>
        </tr>
    </thead>
    <tbody>
        @{ 
            decimal totalCost = 0;
        }
        @foreach (var line in Model)
        {
            <tr>
                <td class="text-center">@line.Product.Name @line.Product.Color</td>
                <td class="text-center">@line.Quantity</td>
                @{ 
                    decimal prod_price = line.Product.Price * ViewBag.Price_coefficient;
                }
                <td class="text-center">@prod_price.ToString("0.00 руб")</td>
                <td class="text-center">@line.Summary.ToString("0.00 руб")</td>
                <td class="text-center">@line.Status</td>
                <td class="text-center">@line.Note</td>
            </tr>

            totalCost += @line.Summary;
        }
    </tbody>
    <tfoot>
        <tr>
            <td colspan="5" class="text-right">Итого:</td>
            <td class="text-right">
                @totalCost.ToString("0.00 руб")
            </td>
        </tr>
    </tfoot>
</table>

